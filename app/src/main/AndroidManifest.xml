<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.grupo.tepeyac.mexico.aic.siembra">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SiembraApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AICSiembra"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.registros.Registros">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.catalogos.Catalogos" />
        <activity android:name=".ui.main.Main" />
        <activity android:name=".ui.login.Login" />
    </application>

</manifest>